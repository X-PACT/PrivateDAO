# ────────────────────────────────────────────────────────────────────
#  PrivateDAO — Environment Configuration
#  Copy this file to .env and fill in your values.
#  Never commit .env to git.
# ────────────────────────────────────────────────────────────────────

# ── Helius RPC (Devnet) ──────────────────────────────────────────────
# Get your free API key at: https://dev.helius.xyz
# Devnet is completely free — no credit card needed.
HELIUS_API_KEY=your_helius_api_key_here

# These are auto-constructed from the key above.
# You can also set them directly if using a different RPC.
SOLANA_RPC_URL=https://devnet.helius-rpc.com/?api-key=${HELIUS_API_KEY}
SOLANA_WSS_URL=wss://devnet.helius-rpc.com/?api-key=${HELIUS_API_KEY}

# ── Network ──────────────────────────────────────────────────────────
# Options: localnet | devnet
SOLANA_CLUSTER=devnet

# ── Wallet ───────────────────────────────────────────────────────────
# Path to your Solana wallet keypair (the deployer/authority wallet)
ANCHOR_WALLET=~/.config/solana/id.json

# ── Program ──────────────────────────────────────────────────────────
PROGRAM_ID=DnQTB3T6xWenyi7LYRsDADfqrKwGJntAaxStaePVkzhs
