import { LegacyRouteRedirect } from "@/components/legacy-route-redirect";

export default function ArenaLegacyPage() {
  return <LegacyRouteRedirect title="Arena review route" description="Arena reviewers should start with the judge hub, then open the live track route and proof packet for each integration." target="/judge" />;
}
