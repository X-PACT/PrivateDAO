import { LegacyRouteRedirect } from "@/components/legacy-route-redirect";

export default function BusinessModelLegacyPage() {
  return <LegacyRouteRedirect title="Business model moved to Revenue" description="The current commercial model, pricing path, and startup positioning are centralized in the Revenue route." target="/revenue" />;
}
