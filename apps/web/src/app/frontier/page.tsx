import { LegacyRouteRedirect } from "@/components/legacy-route-redirect";

export default function FrontierLegacyPage() {
  return <LegacyRouteRedirect title="Frontier track route" description="The current Frontier closure and encrypted partner tracks are available from the live services router." target="/services/main-frontier-closure" />;
}
