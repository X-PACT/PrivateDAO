import { LegacyRouteRedirect } from "@/components/legacy-route-redirect";

export default function IntegrationsLegacyPage() {
  return <LegacyRouteRedirect title="Integrations moved to Services" description="Partner integrations are now presented as runnable service lanes with proof links and Testnet boundaries." target="/services" />;
}
