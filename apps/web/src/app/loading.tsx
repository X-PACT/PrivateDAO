import { RouteLoadingShell } from "@/components/route-loading-shell";

export default function Loading() {
  return (
    <RouteLoadingShell
      eyebrow="Loading PrivateDAO"
      title="Preparing the live product surface"
      description="PrivateDAO is loading wallet-ready controls, Testnet context, and proof-linked routes so the app lands with real content instead of an empty frame."
    />
  );
}
