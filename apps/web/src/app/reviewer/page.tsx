import { LegacyRouteRedirect } from "@/components/legacy-route-redirect";

export default function ReviewerLegacyPage() {
  return <LegacyRouteRedirect title="Reviewer links are preserved" description="Continue to the canonical judge hub for the fastest route through product value, encrypted execution, and Testnet evidence." target="/judge" />;
}
