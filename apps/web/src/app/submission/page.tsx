import { LegacyRouteRedirect } from "@/components/legacy-route-redirect";

export default function SubmissionLegacyPage() {
  return <LegacyRouteRedirect title="Submission route consolidated" description="The active submission links for Jupiter, Umbra, Eitherway, Encrypt/Ika, and consumer governance now live in the judge hub." target="/judge" />;
}
