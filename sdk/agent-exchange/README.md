# PrivateDAO Agent Exchange SDKs

Production base URL: `https://agents.privatedao.org`.

The clients support discovery, free verification, paid job creation, payment-intent retrieval, signature submission, polling, receipts, agent registry, logistics, and discovery. The SDK never holds a wallet key or signs a transaction.

Start with the developer guide at `https://agents.privatedao.org/connect`.
